import { FinanceShell } from '@/components/finance-shell'; import { MvpUtilityContent } from '@/components/mvp-utility-content'; import { getChatGPTUser } from '../chatgpt-auth'; export const dynamic='force-dynamic'; export default async function Page(){const user=await getChatGPTUser();return <FinanceShell active="documents" userName={user?.displayName??'Jordan Lee'} userEmail={user?.email??'finance.manager@northstar.demo'} demo={!user}><MvpUtilityContent kind="documents"/></FinanceShell>}
